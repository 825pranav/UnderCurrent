# UnderCurrent — Python dependencies
#
# Core pipeline (simulate mode): Python 3.8+ stdlib only — no packages needed.
#
# ── DASHBOARDS ───────────────────────────────────────────────────────────────
streamlit>=1.30.0
pandas>=1.5.0
plotly>=5.0.0
#   pip install streamlit pandas plotly
#   python3 run.py                          # React dashboard (http://localhost:5050)
#   python3 streamlit_dash.py              # Streamlit dashboard (http://localhost:8501)
#
# ── WASM POLICY SANDBOX ───────────────────────────────────────────────────────
wasmtime>=22.0.0
#   pip install wasmtime
#   Required by stateful/wasm_executor.py. Falls back to Python policy if absent.
#
# ── REAL eBPF MODE (Linux + root only) ───────────────────────────────────────
# bcc is a system package — not installable via pip.
#   Ubuntu/Debian:  sudo apt-get install bpfcc-tools python3-bpfcc linux-headers-$(uname -r)
#   Fedora/RHEL:    sudo dnf install bcc bcc-tools python3-bcc kernel-devel
#   Then run:       sudo python3 run.py --real
#
# ── DOCKER ACTIONS ────────────────────────────────────────────────────────────
# Requires Docker daemon for `docker restart`. Fails gracefully without it.
