# UnderCurrent — Python dependencies
#
# ── CORE PIPELINE (main.py) ──────────────────────────────────────────────────
# Simulate mode: zero external packages — Python 3.8+ stdlib only.
#   python3 stateless/main.py
#
# ── TERMINAL DASHBOARD (dashboard.py) ────────────────────────────────────────
rich>=13.0.0
#   pip install rich
#   python3 stateless/dashboard.py
#
# ── WEB DASHBOARD (streamlit_dashboard.py) ───────────────────────────────────
streamlit>=1.30.0
pandas>=1.5.0
plotly>=5.0.0
#   pip install streamlit pandas plotly
#   streamlit run stateless/streamlit_dashboard.py
#
# ── REAL MODE (eBPF, Linux + root only) ──────────────────────────────────────
# bcc is a SYSTEM package — not installable via pip.
#
#   Ubuntu / Debian:
#     sudo apt-get install bpfcc-tools python3-bpfcc linux-headers-$(uname -r)
#
#   Fedora / RHEL:
#     sudo dnf install bcc bcc-tools python3-bcc kernel-devel
#
#   Then run: sudo python3 stateless/main.py --real
#
# ── DOCKER ACTIONS (optional) ────────────────────────────────────────────────
# Requires Docker daemon running on the host for `docker restart` to work.
# Without Docker, restart actions will fail gracefully (logged, not fatal).
#   Install: https://docs.docker.com/engine/install/
